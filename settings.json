{
  // The following, 'identifier', 'password', 'sourceurl' and X-IG-API-KEY are supplied to you when you register to use IG API on the IG web site
  "sources": [
    {
      "token": "", // Leave this element blank and in place
      "key": "demo", // This is passed in as an argument when running application and determines which source you use, must be unique
      "identifier": "", // IG API username
      "password": "", // IG API password
      "sourceurl": "https://demo-api.ig.com", // Point to DEMO or LIVE url
      "X-IG-API-KEY": "56cd58a628b4f7fb494b54bfd2668d91e55fba74"
    }
  ],
  "shortcodes": [
    {
      "key": "t1",
      "value": "train oscillator signal"
    }
  ]
}